# Tasks: WhatsApp Channel

**Input**: Design documents from `/specs/029-whatsapp-channel/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/whatsapp-api.md, quickstart.md

**Tests**: Included (Constitution VII requires them; run narrow per-crate suites with PostgreSQL up — never rely on the workspace-wide gate, it skips DB tests).

**Organization**: Tasks are grouped by user story. Each task is deliberately small and names the exact file plus an existing file to copy the pattern from. Read the named pattern file before writing code.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: US1 = connect channel, US2 = inbound→inbox, US3 = agent replies, US4 = AI replies

## Path Conventions

Backend Rust workspace at `backend/` (module crates in `backend/crates/modules/`), Angular dashboard at `frontend/apps/dashboard/`. All paths below are repo-relative.

---

## Phase 1: Setup

**Purpose**: Create the empty `whatsapp` module crate and wire it into the workspace.

- [X] T001 Create `backend/crates/modules/whatsapp/Cargo.toml` (copy `backend/crates/modules/widgets/Cargo.toml` as the starting point; package name `whatsapp`; dependencies: `axum`, `sqlx`, `serde`, `serde_json`, `tokio`, `tracing`, `uuid`, `chrono`, `hex`, `hmac`, `sha2`, `base64`, `rand`, `reqwest` (workspace, `json` feature), plus path deps `kernel`, `config`, `storage`, `integrations`, `conversations`, `customers` — mirror how widgets declares its path deps) and `backend/crates/modules/whatsapp/src/lib.rs` declaring `pub mod api; pub mod identity; pub mod inbound; pub mod media; pub mod model; pub mod queries; pub mod routes; pub mod sender; pub mod webhook; pub mod window;` with empty placeholder files for each so `cargo check -p whatsapp` passes. If `backend/Cargo.toml` lists workspace members explicitly, add `crates/modules/whatsapp`.
- [X] T002 Add `whatsapp = { path = "../modules/whatsapp" }` to `backend/crates/server/Cargo.toml` dependencies so the server crate can mount routes and spawn workers later; `cargo check -p server` must still pass.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Database schema, shared row types/queries, the Graph API trait, and integrations-crate helpers every story needs.

**⚠️ CRITICAL**: Complete this phase before starting any user story.

- [X] T003 Create migration `backend/migrations/0057_whatsapp_channel.sql` implementing exactly what `specs/029-whatsapp-channel/data-model.md` specifies: (1) idempotent DO-block adding `messages_tenant_id_id_uq UNIQUE (tenant_id, id)` on `messages` (copy the DO-block style from `backend/migrations/0034_messages.sql`); (2) table `whatsapp_message_meta` with all columns, CHECKs, the partial unique index on `(tenant_id, wamid) WHERE wamid IS NOT NULL`, unique index on `(message_id)`, index on `(tenant_id, conversation_id)`, and an `updated_at` trigger (copy trigger style from `backend/migrations/0050_website_chat_widget.sql`); (3) table `message_attachments` with all columns, CHECKs, index on `(tenant_id, message_id)`, partial index on `(status, updated_at) WHERE status = 'pending'`, and updated_at trigger; (4) INSERT into `integration_catalog` for slug `whatsapp` (name `WhatsApp`, category `messaging`, `is_available = true`, `config_schema` with the five fields listed in data-model.md §1 — copy the jsonb formatting of the `generic-webhook` seed in `backend/migrations/0056_integrations_foundation.sql`).
- [X] T004 [P] Write `backend/crates/modules/whatsapp/src/model.rs`: structs `WhatsappMessageMetaRow` and `MessageAttachmentRow` matching the 0057 columns (derive `sqlx::FromRow`, `serde::Serialize` where useful — copy struct style from `backend/crates/modules/integrations/src/model.rs`); string-enum helpers `DeliveryStatus` (`pending|sent|delivered|read|failed`) with `fn rank(&self) -> u8` (pending=0, sent=1, delivered=2, read=3, failed=99) for monotonic updates, and `AttachmentKind` (`image|audio|video|document`); serde structs for the Meta webhook envelope (`WebhookEnvelope → entry[] → changes[] → value { messages[], statuses[], contacts[] }`) with only the fields the contract needs: message `id`, `from`, `timestamp`, `type`, `text.body`, media objects (`id`, `mime_type`, `caption`, `filename`), status `id`, `status`, `errors[] { code, title }`, contact `profile.name` + `wa_id` — mark everything `#[serde(default)]`-tolerant so unknown payloads never fail parsing.
- [X] T005 Write `backend/crates/modules/whatsapp/src/queries.rs` with these functions (copy SQLx style from `backend/crates/modules/integrations/src/queries.rs`; every query filters by `tenant_id`): `insert_inbound_meta_in_tx(tx, tenant_id, message_id, conversation_id, wamid, provider_timestamp) -> sqlx::Result<bool>` using `ON CONFLICT DO NOTHING` returning false when the wamid already existed (dedupe signal — do it as `INSERT ... ON CONFLICT (tenant_id, wamid) WHERE wamid IS NOT NULL DO NOTHING` + `rows_affected() == 0` check via a pre-SELECT on the partial index, see note in file); `insert_outbound_meta_in_tx(tx, tenant_id, message_id, conversation_id) -> Result<()>` (status `pending`); `set_outbound_sent_in_tx(tx, tenant_id, message_id, wamid)`; `set_outbound_failed_in_tx(tx, tenant_id, message_id, reason)`; `update_status_by_wamid_in_tx(tx, tenant_id, wamid, new_status, failure_reason) -> Result<Option<(Uuid, Uuid)>>` returning `(conversation_id, message_id)` only when the update applied, enforcing monotonicity in SQL (`WHERE ... AND <rank of stored status> < <rank of new status>` expressed with a CASE expression); `insert_attachment_in_tx(tx, tenant_id, message_id, kind, provider_media_id, file_name) -> Result<Uuid>`; `claim_pending_attachment(pool) -> Result<Option<MessageAttachmentRow>>` using `FOR UPDATE SKIP LOCKED` + `fetch_attempts < 5` (copy the claim shape from `backend/crates/modules/ai/src/agent_responder.rs` step 1); `mark_attachment_stored(pool, id, storage_key, mime, size)`; `mark_attachment_failed_attempt(pool, id)` (increments `fetch_attempts`, sets `failed` when it reaches 5); `attachment_for_download(pool, tenant_id, conversation_id, attachment_id) -> Result<Option<MessageAttachmentRow>>` (joins `messages` to check the attachment belongs to that conversation, returns only `status='stored'` rows).
- [X] T006 [P] Write `backend/crates/modules/whatsapp/src/api.rs`: `#[async_trait] pub trait WhatsAppApi: Send + Sync` with `async fn send_text(&self, access_token: &str, phone_number_id: &str, to_e164: &str, body: &str) -> Result<String, SendError>` (returns wamid), `async fn media_url(&self, access_token: &str, media_id: &str) -> Result<MediaInfo, SendError>` (`MediaInfo { url, mime_type }`), `async fn download(&self, access_token: &str, url: &str) -> Result<bytes::Bytes, SendError>`; enum `SendError { WindowExpired, Unauthorized, Transient(String), Other(String) }` — map Graph error code `131047` → `WindowExpired`, HTTP 401/403 → `Unauthorized`, 5xx/network → `Transient`; struct `GraphWhatsAppApi { http: reqwest::Client, base_url: String }` implementing it against `{base_url}/{phone_number_id}/messages` etc. (base_url default `https://graph.facebook.com/v23.0`, overridable for tests — copy reqwest usage style from `backend/crates/modules/tools/src/`); and `pub struct MockWhatsAppApi` (behind `#[cfg(any(test, feature = "test-util"))]` or just always compiled, simplest wins) whose responses are programmable via `Mutex<Vec<...>>` queues for tests.
- [X] T007 In `backend/crates/modules/integrations/src/lib.rs` and the files it re-exports, make the helpers the whatsapp crate needs public: `crypto::MasterKey` + decrypt function, `webhook::hash_token`, and add two new pub query helpers in `backend/crates/modules/integrations/src/queries.rs`: `connection_by_token_and_slug(pool, token_hash, slug) -> Result<Option<ConnectionRow-ish>>` (active connection matching both the webhook token hash and catalog slug, returning tenant_id + connection_id + config jsonb) and `decrypted_secret(pool, master_key, connection_id, field_key) -> Result<Option<String>>`; also `active_connection_for_slug(pool, tenant_id, slug) -> Result<Option<Uuid>>` (used by the reply guard and sender). Reuse existing private query internals — do not duplicate SQL that already exists; `cargo test -p integrations` must stay green.

**Checkpoint**: `cargo check -p whatsapp -p server` passes, migration applies cleanly, foundation ready.

---

## Phase 3: User Story 1 - Connect a WhatsApp business number (Priority: P1) 🎯 MVP

**Goal**: Tenant admins connect/disconnect WhatsApp via the existing integrations pages (catalog entry ships in T003); Meta's GET verification handshake works against the connection.

**Independent Test**: quickstart.md §1 — connect with test values in the dashboard, see status connected + masked secrets + webhook URL; curl the handshake and get `hub.challenge` echoed; wrong verify_token → uniform 404.

- [X] T008 [US1] Write the GET handshake handler in `backend/crates/modules/whatsapp/src/webhook.rs`: `pub async fn verify_subscription(...)` for `GET /integrations/whatsapp/webhook/{token}` — hash the path token with `integrations::webhook::hash_token`, look up the active connection with `connection_by_token_and_slug(…, "whatsapp")` (T007), decrypt the `verify_token` secret, and when `hub.mode == "subscribe"` && `hub.verify_token` matches, respond `200` `text/plain` with `hub.challenge` verbatim; every other outcome returns the byte-identical `404 {"error":"not found"}` (copy `not_found_response()` from `backend/crates/modules/integrations/src/webhook.rs`). Also add `pub fn public_router() -> axum::Router<AppState-equivalent>` in `backend/crates/modules/whatsapp/src/routes.rs` registering the GET route (copy router-building style from `backend/crates/modules/widgets/src/lib.rs` / how widgets exposes its public routes).
- [X] T009 [US1] Mount the whatsapp public router in `backend/crates/server/src/main.rs` next to where the integrations webhook route is mounted (search for `integrations` router nesting) so `GET /integrations/whatsapp/webhook/{token}` is live; `cargo check -p server` passes.
- [X] T010 [P] [US1] In `frontend/apps/dashboard/src/app/features/tenant/integrations/integration-detail.component.ts` (and its store/API service if the URL is built there), find where the webhook URL for a connection is rendered and make the base path slug-aware: slug `whatsapp` → `/integrations/whatsapp/webhook/{token}`, every other slug keeps the current generic path. Keep it a small pure function so both pages reuse it.
- [X] T011 [US1] Backend tests in `backend/crates/modules/whatsapp/tests/handshake_tests.rs` (DB required; copy test-harness setup — pool creation, tenant fixture, connecting an integration — from `backend/crates/modules/integrations/tests/`): handshake echoes challenge on correct verify_token; wrong verify_token, wrong mode, unknown token, and disconnected connection all return 404 with identical bodies. Run `cargo test -p whatsapp`.

**Checkpoint**: US1 fully demoable via quickstart §1.

---

## Phase 4: User Story 2 - Incoming WhatsApp messages become inbox conversations (Priority: P2)

**Goal**: Signed POST deliveries create/update customers, conversations, messages, and media attachments; conversations appear in the inbox with a WhatsApp badge.

**Independent Test**: quickstart.md §2 — signed curl creates customer + conversation + message; identical resend is deduped; phone-identity attach works; bad signature rejected with no side effects; media message shows an attachment.

- [X] T012 [P] [US2] Add customer-identity helpers to `backend/crates/modules/customers/src/queries.rs` (copy the style of `create_anonymous_customer_in_tx` in the same file): `find_customer_by_identifier_in_tx(tx, tenant_id, channel, identifier) -> Result<Option<Uuid>>` (live rows only: `deleted_at IS NULL`); `attach_identifier_in_tx(tx, tenant_id, customer_id, channel, identifier) -> Result<()>` using `ON CONFLICT DO NOTHING` against the live-unique index; `create_customer_with_identifier_in_tx(tx, tenant_id, display_name, channel, identifier) -> Result<Uuid>` (insert customer + identifier row in the same tx). `cargo check -p customers` passes.
- [X] T013 [P] [US2] Add `find_open_conversation_in_tx(tx, tenant_id, customer_id, channel) -> Result<Option<Uuid>>` to `backend/crates/modules/conversations/src/queries.rs`: newest conversation with that channel, `status IN ('open','escalated')`, `deleted_at IS NULL`, ordered by `last_activity_at DESC LIMIT 1`.
- [X] T014 [US2] Write `backend/crates/modules/whatsapp/src/identity.rs`: `pub fn normalize_wa_id(wa_id: &str) -> String` (strip non-digits, prefix `+`) with unit tests in the same file (`#[cfg(test)]`), and `pub async fn resolve_customer_in_tx(tx, tenant_id, wa_id, profile_name) -> Result<Uuid>` implementing research.md R8 exactly: try `whatsapp` identifier → try `phone` identifier (then attach a `whatsapp` identifier) → else create customer (display name = profile_name or the normalized number), using only the T012 helpers.
- [X] T015 [US2] Write `backend/crates/modules/whatsapp/src/inbound.rs`: `pub async fn process_message(pool, tenant_id, msg: &IncomingMessage, contact: Option<&Contact>) -> Result<()>` — one transaction that: (1) skips if `insert_inbound_meta_in_tx` reports the wamid already seen (insert meta LAST, see order note below — simplest correct order: do a `SELECT 1 FROM whatsapp_message_meta WHERE tenant_id=$1 AND wamid=$2` first and return early, then rely on the unique index at insert time to catch races); (2) resolves the customer via `identity::resolve_customer_in_tx`; (3) finds-or-creates the conversation via `find_open_conversation_in_tx` / `conversations::queries::create_conversation_in_tx` with channel `whatsapp`; (4) inserts the message via `conversations::queries::add_message_in_tx` kind `customer` — body = text body, or media caption, or a placeholder like `[Image]`/`[Audio]`/`[Video]`/`[Document]`/`[Location]`/`[Contact]`/`[Sticker]`/`[Unsupported message]`; (5) for media types inserts a `message_attachments` row (kind + provider media id + filename) via T005; (6) inserts the inbound meta row (wamid + provider timestamp); (7) emits `conversations::outbox::emit_customer_message_in_tx(tx, tenant, conversation, message, "whatsapp")`; commit.
- [X] T016 [US2] Add the POST intake handler to `backend/crates/modules/whatsapp/src/webhook.rs` and register it in `routes.rs`: `POST /integrations/whatsapp/webhook/{token}` — copy the overall shape of `backend/crates/modules/integrations/src/webhook.rs` (raw `Bytes` body, per-connection `InMemoryRateLimitStore` 60/min, 256KB size cap, uniform 404, throttled rejection events) but verify `X-Hub-Signature-256: sha256=<hex>` with HMAC-SHA256 keyed by the decrypted `app_secret` secret (constant-time via `Mac::verify_slice`, exactly like the existing `verify_signature` — write `verify_meta_signature(secret, raw_body, header)` in this file with unit tests); on success insert an `integration_webhook_deliveries` row + `delivery_accepted` event (reuse integrations queries from T007 exposure), parse the envelope (T004 structs), call `inbound::process_message` for each message entry (statuses handled in US3 — ignore here with a `tracing::debug!`), respond `200 {"received":true}`. Malformed JSON → `400` + `malformed_payload` rejection event.
- [X] T017 [US2] Write the media worker in `backend/crates/modules/whatsapp/src/media.rs`: `pub async fn run_media_fetch_worker(pool, api: Arc<dyn WhatsAppApi>, config)` — loop every ~2s (copy the worker loop shape from `backend/crates/modules/ai/src/agent_responder.rs`'s `run_..._worker`): claim one pending attachment (T005), load its tenant's whatsapp connection `access_token` (T007 helpers; if the connection is gone → `mark_attachment_failed_attempt`), call `api.media_url` then `api.download`, upload via the `storage` shared crate (copy the put pattern from `backend/crates/modules/knowledge/src/upload.rs` `put_then_persist`) to key `whatsapp-media/{tenant_id}/{attachment_id}`, then `mark_attachment_stored` with mime + byte length; any error → `mark_attachment_failed_attempt`. Spawn it in `backend/crates/server/src/main.rs` beside the other workers (construct `Arc<GraphWhatsAppApi>` there).
- [X] T018 [US2] Add the attachment download endpoint in `backend/crates/modules/whatsapp/src/routes.rs`: `GET /tenant/conversations/{conversation_id}/attachments/{attachment_id}` behind the same tenant-auth/permission layer the conversations tenant routes use (find how `conversations` routes declare their permission middleware and copy it; require the conversation-view permission) — fetch via `attachment_for_download` (T005; returns None → uniform 404), stream the object from `storage` with `Content-Type` = stored mime and, for `kind='document'`, `Content-Disposition: attachment; filename="..."`. Mount in `backend/crates/server/src/main.rs` under the tenant router.
- [X] T019 [US2] Extend the conversation timeline response in `backend/crates/modules/conversations/src/queries.rs` + the timeline DTO (find the struct serialized by `timeline_query_in_tx` / the routes handler): add batch loaders `load_attachments_for_messages(pool, tenant_id, &[message_ids])` and `load_whatsapp_meta_for_messages(pool, tenant_id, &[message_ids])` (copy the batching pattern of `load_citations_for_messages` in the same file; these read whatsapp-owned tables directly — add a comment citing the notifications hand-rolled-emit precedent for why this cross-module read is deliberate), and attach optional `attachments: [{id, kind, status, mimeType, sizeBytes, fileName, url}]` (url = `/tenant/conversations/{cid}/attachments/{aid}` when `stored`, else null) and `delivery: {status, failureReason}` (outbound meta only; a whatsapp `reply`/`ai` message with no meta row yet serializes as `{status: "pending"}`) fields per `contracts/whatsapp-api.md` §2.
- [X] T020 [P] [US2] Update `frontend/apps/dashboard/src/app/features/tenant/conversations/conversations-api.service.ts` (and any message model/interface files it imports) with the new optional `attachments` and `delivery` fields, typed exactly as the contract JSON.
- [X] T021 [P] [US2] Add WhatsApp to the channel presentation in the inbox: find how `channel` is displayed/filtered in `frontend/apps/dashboard/src/app/features/tenant/conversations/inbox-list.component.ts`, `conversation-summary.component.ts`, and `conversations.component.ts` (search those files for `web_chat`) and add a `whatsapp` badge/label/filter option in the same style — no new UI patterns, reuse the existing badge component/token.
- [X] T022 [US2] Render attachments in `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-thread.component.ts` as a small standalone `message-attachment` component (new file `frontend/apps/dashboard/src/app/features/tenant/conversations/message-attachment.component.ts`, standalone, OnPush, follow the visual conventions of neighboring components): `image` → inline `<img>` capped width linking to the url; `audio`/`video` → native `<audio controls>`/`<video controls>`; `document` → download link with file name; `status='pending'` → subtle "receiving…" placeholder; `status='failed'` → muted failure note. Unit test alongside as `.spec.ts` following the existing spec style in that folder.
- [X] T023 [US2] Backend integration tests in `backend/crates/modules/whatsapp/tests/intake_tests.rs` (reuse the T011 harness): signed delivery for an unknown number creates customer + whatsapp identifier + open conversation + customer message + `conversation.customer_message` outbox row with channel `whatsapp`; second message from the same number appends to the same conversation; identical redelivery creates nothing new (SC-003); number matching an existing `phone` identifier attaches to that customer without a new profile; message after the conversation is closed opens a new conversation; wrong signature → 404 and zero rows written; image message creates a pending attachment row. Run `cargo test -p whatsapp -p conversations -p customers`.

**Checkpoint**: US1 + US2 demoable — inbound WhatsApp traffic lands in the inbox.

---

## Phase 5: User Story 3 - Human agents reply through WhatsApp (Priority: P3)

**Goal**: Agent replies dispatch through the Graph API with full sent/delivered/read/failed lifecycle, window enforcement, and clear failures.

**Independent Test**: quickstart.md §3 — reply shows pending→sent with the mock API; simulated status webhook flips it to read (and a late `delivered` does not downgrade); backdated conversation → `422 whatsapp_window_expired`; disconnected channel → `422 whatsapp_channel_disconnected`.

- [X] T024 [P] [US3] Add `emit_whatsapp_outbound_in_tx(tx, tenant_id, conversation_id, message_id)` to `backend/crates/modules/conversations/src/outbox.rs` inserting an `outbox_events` row with `event_type = 'whatsapp.outbound_message'` and payload `{tenantId, conversationId, messageId}` (copy `emit_customer_message_in_tx` in the same file).
- [X] T025 [P] [US3] Add `last_customer_message_at(pool_or_tx, tenant_id, conversation_id) -> Result<Option<DateTime<Utc>>>` to `backend/crates/modules/conversations/src/queries.rs` (max `created_at` of kind `customer` messages; uses the existing timeline index).
- [X] T026 [US3] Guard + emit in the reply path of `backend/crates/modules/conversations/src/routes.rs` (the handler that calls `add_message_in_tx` with kind `reply` — near `conversations/src/routes.rs:745`): when the conversation's channel is `whatsapp`, before inserting: return `422` ApiError code `whatsapp_channel_disconnected` if `integrations::queries::active_connection_for_slug(pool, tenant, "whatsapp")` (T007) is None; `422` code `whatsapp_window_expired` ("The WhatsApp messaging window has expired for this conversation.") if `last_customer_message_at` is None or older than 24h; `422` code `whatsapp_body_too_long` if body > 4096 chars. After a successful insert, call `emit_whatsapp_outbound_in_tx` in the same transaction. Match the existing ApiError/validation-error construction style used elsewhere in the file.
- [X] T027 [P] [US3] Write `backend/crates/modules/whatsapp/src/window.rs`: `pub const WINDOW: chrono::Duration` (24h) and `pub fn window_open(last_customer_at: Option<DateTime<Utc>>, now: DateTime<Utc>) -> bool`, with unit tests (exact boundary: 23h59m open, 24h01m closed, None closed).
- [X] T028 [US3] Write the sender worker in `backend/crates/modules/whatsapp/src/sender.rs`: `pub async fn run_whatsapp_sender_worker(pool, api: Arc<dyn WhatsAppApi>, config)` — claim one `whatsapp.outbound_message` outbox event with the exact `FOR UPDATE SKIP LOCKED` claim/delete pattern of `backend/crates/modules/ai/src/agent_responder.rs`; then: load message body + conversation + the customer's `whatsapp` identifier (query via a small helper in `whatsapp/src/queries.rs`); `insert_outbound_meta_in_tx` (pending) if absent; re-check the window with `window::window_open` + `last_customer_message_at` → on closed, `set_outbound_failed_in_tx` with the window reason; load connection config (`phone_number_id`) + decrypted `access_token` (T007) → missing/inactive connection fails the message with a disconnected reason; call `api.send_text` → `Ok(wamid)` → `set_outbound_sent_in_tx`; `Err(WindowExpired)` → failed + window reason; `Err(Unauthorized)` → failed + "WhatsApp credentials rejected — reconnect the integration" AND insert an `integration_events` failure row so the connection health degrades; `Err(Transient)` → leave the event unclaimed for retry (max 5 attempts tracked in the event payload, then failed). After any terminal meta write, emit a `conversation.message_status` outbox event (`{tenantId, conversationId, messageId, status, failureReason}` — hand-rolled insert like `emit_whatsapp_outbound_in_tx`). Spawn the worker in `backend/crates/server/src/main.rs`.
- [X] T029 [US3] Handle `statuses[]` in the POST intake handler (`backend/crates/modules/whatsapp/src/webhook.rs`, extending T016): for each status entry call `update_status_by_wamid_in_tx` (monotonic — T005); when it applies, emit the same `conversation.message_status` outbox event; unknown wamid → `tracing::debug!` and continue; a `failed` status stores the first error title as `failure_reason`.
- [X] T030 [US3] Forward `conversation.message_status` outbox events to the tenant SSE stream: in `backend/crates/modules/escalations/src/events.rs`, add the event type to the worker's claimed-types list (the SQL near `backend/crates/modules/escalations/src/events.rs:279`) and add a match arm serializing it as SSE event `conversation.message_status` (copy an adjacent arm exactly).
- [X] T031 [P] [US3] Frontend delivery status: extend the message types (T020 file) if needed, then render ticks in `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-thread.component.ts` for outbound messages with a `delivery` field — pending (clock), sent (✓), delivered (✓✓), read (✓✓ accent), failed (⚠ + `failureReason` tooltip/inline note) — as a tiny presentational piece inside the thread; subscribe to the `conversation.message_status` SSE event in `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-detail.store.ts` via the existing tenant-events realtime client (find it by searching the dashboard app for `escalation.assigned`) and patch the matching message's `delivery` signal state.
- [X] T032 [P] [US3] Composer errors: in `frontend/apps/dashboard/src/app/features/tenant/conversations/composer.component.ts` (and the store handling send failures), surface the three 422 codes from `contracts/whatsapp-api.md` §2 as inline human-readable messages (window expired / channel disconnected / message too long), following however the composer surfaces validation errors today.
- [X] T033 [US3] Backend tests: `backend/crates/modules/whatsapp/tests/sender_tests.rs` with `MockWhatsAppApi` — success path writes sent + wamid and emits `conversation.message_status`; WindowExpired error → failed with window reason; Unauthorized → failed + integration failure event; status webhook `read` then late `delivered` keeps `read` (monotonicity); and in `backend/crates/modules/conversations/tests/` add reply-guard cases (window expired 422, disconnected 422, >4096 422, success emits `whatsapp.outbound_message`). Run `cargo test -p whatsapp -p conversations`.

**Checkpoint**: two-way WhatsApp conversations work end-to-end with lifecycle tracking.

---

## Phase 6: User Story 4 - AI responds automatically on WhatsApp (Priority: P4)

**Goal**: AI replies (existing pipeline) get dispatched over WhatsApp; escalation/failure behavior unchanged.

**Independent Test**: quickstart.md §4 — inbound message with AI handling + `whatsapp` enabled channel produces an `ai` message AND a `whatsapp.outbound_message` event the sender dispatches; escalation flows unchanged.

- [X] T034 [US4] In `backend/crates/modules/ai/src/agent_responder.rs`, after every code path that inserts an outbound AI-visible message into the conversation (`insert_ai_reply_in_tx` and `insert_fallback_in_tx` call sites — search the file for both), when the triggering event's `channel == "whatsapp"`, call `conversations::outbox::emit_whatsapp_outbound_in_tx` inside the same transaction as the insert (the `ai` crate already depends on `conversations`, so this is a normal call, not a new dependency). Do NOT emit for the auto-ack path only if it's outside AI handling — actually DO emit for `insert_auto_ack_in_tx` too when channel is whatsapp (the customer must receive the acknowledgment on WhatsApp); that call site is in the same file.
- [X] T035 [US4] Integration test `backend/crates/modules/whatsapp/tests/ai_reply_tests.rs` (reuse harness + whatever AI test doubles `backend/crates/modules/ai/tests/` already use): seed an agent configuration with `whatsapp` in `enabled_channels`, process an inbound delivery, run one responder tick, assert an `ai` message exists and a `whatsapp.outbound_message` outbox row references it; second case: tenant with no agent config → auto-ack system message also gets an outbound event. Run `cargo test -p whatsapp -p ai`.

**Checkpoint**: all four stories independently functional.

---

## Phase 7: Polish & Cross-Cutting Concerns

- [X] T036 [P] Write module documentation in `backend/crates/modules/whatsapp/src/lib.rs` following the header format of `backend/crates/modules/customers/src/lib.rs` (Purpose, Responsibilities, Public Interfaces, Dependencies, Data Model incl. migration 0057, Extension Points — note outbound media + template messages as future extensions).
- [X] T037 [P] Add a `029-whatsapp-channel` line to the Recent Changes lists in `CLAUDE.md` and `AGENTS.md` (one sentence each, matching the style of the existing 027/028 lines).
- [X] T038 Run the full validation: `cargo test -p whatsapp -p conversations -p customers -p integrations -p ai` with PostgreSQL up, `npx nx test dashboard` in `frontend/`, then walk `specs/029-whatsapp-channel/quickstart.md` §§1–4 manually. Do not run or trust the workspace-wide `cargo test --workspace` gate, and do not "fix" pre-existing repo-wide `cargo fmt` differences — format only files this feature touched.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: none.
- **Phase 2 (Foundational)**: needs Phase 1. T003 blocks T005; T007 is independent of T003–T006.
- **US1 (Phase 3)**: needs Phase 2 (T003, T007). Delivers the MVP connect + handshake.
- **US2 (Phase 4)**: needs Phase 2; independent of US1's tasks except sharing `webhook.rs`/`routes.rs` files (T016/T018 touch files T008 created — do US1 first or coordinate).
- **US3 (Phase 5)**: needs US2's intake (T016) for status handling (T029) and the timeline fields (T019) for the UI; T024/T025/T027 can start right after Phase 2.
- **US4 (Phase 6)**: needs T024 (the emit function) and US3's sender to actually deliver; the T034 code change itself only needs T024.
- **Phase 7 (Polish)**: last.

### Within-story ordering

Queries/helpers → services/handlers → server wiring → frontend → tests (tests may be written first if you prefer TDD; they must pass by the end of the story).

## Parallel Example: User Story 2

```text
# After Phase 2, these US2 tasks touch disjoint files and can run in parallel:
T012 (customers/src/queries.rs)   T013 (conversations/src/queries.rs)
T020 (conversations-api.service.ts)   T021 (inbox components)
# Then sequentially: T014 → T015 → T016 → T017/T018 → T019 → T022 → T023
```

## Implementation Strategy

**MVP first**: Phases 1–3 give a demoable connect + verified handshake (US1). Each later phase is an independently testable increment: US2 = inbound traffic in the inbox, US3 = two-way messaging, US4 = AI on the channel. Stop and run the story's quickstart section at every checkpoint before moving on.

**Notes for the implementing model**:
- Always open and read the named "copy the pattern from" file before writing code; the codebase's existing idioms are the spec for style, error handling, and permissions.
- Every SQL query must filter by `tenant_id`. Every new endpoint needs the same auth/permission middleware as its neighbors.
- Backend tests need a running PostgreSQL; run the narrow `-p` suites named in each test task, never the workspace-wide gate.
- Commit after each task or small logical group.

---

## Phase 8: Convergence

**Why**: The whatsapp crate is fully built and unit-tested, but a codebase assessment found it is **not connected to the running server** and several delivery/UI legs are unwired, so the feature does nothing at runtime. Each task below is self-contained — do NOT rely on the earlier `[X]` tasks being complete; the imperative body is the full spec, and the `(orig Txxx)` note is only provenance. Every SQL query filters by `tenant_id`; open and read each named "copy from" file before writing.

- [X] T039 [CRITICAL] Wire the `whatsapp` crate into the running server (orig T002/T009/T017/T018/T028-spawn — currently the `server` crate has ZERO references to `whatsapp`, so the webhook, attachment endpoint, and both workers are dead). Do ALL of these, in order:
  1. **Dependency**: add `whatsapp = { path = "../modules/whatsapp" }` to `[dependencies]` in `backend/crates/server/Cargo.toml` (next to `integrations`/`widgets`).
  2. **Public webhook router**: in `backend/crates/server/src/router.rs`, mount `whatsapp::routes::public_router()` so `GET`/`POST /integrations/whatsapp/webhook/{token}` are live. It MUST sit OUTSIDE the tenant/auth middleware — mount it exactly where the integrations webhook is mounted (search router.rs for `integrations::webhook::receive_webhook`, around line 120, comment "Mounted outside the tenant middleware"). **State gotcha**: `public_router()` returns `Router<PgPool>`, but the server builds `Router<AppState>`. Adapt state the same way the `/api/v1` router does at router.rs:1097 — call `.with_state(state.db.clone())` on the whatsapp router to turn it into a `Router<()>`, then `.merge(...)` it into the top-level `Router` built at router.rs:~1071 (the `let mut router: Router<AppState> = ...` chain), NOT into the `AppState`-typed api routers. `state.db` is the `PgPool` field (see `backend/crates/server/src/state.rs`).
  3. **Tenant attachment route**: mount `whatsapp::routes::tenant_router()` (which serves `GET /conversations/{conversation_id}/attachments/{attachment_id}`) UNDER the tenant middleware stack so it becomes `/tenant/conversations/...` and is auth-protected — merge it into the `tenant` router that gets the `from_fn_with_state(...)` principal/tenant layers at router.rs:~985–990, applying `.with_state(state.db.clone())` the same way as step 2. Confirm the final path matches the contract: `GET /tenant/conversations/{conversation_id}/attachments/{attachment_id}`.
  4. **Media worker**: in `backend/crates/server/src/main.rs`, after `storage` is constructed (around line 83) and next to the other `tokio::spawn(... _worker(...))` calls (e.g. `agent_responder_worker` at ~line 103), spawn `whatsapp::media::run_media_fetch_worker(state.db.clone(), api.clone(), storage.clone(), master_key.clone())`.
  5. **Sender worker**: spawn `whatsapp::sender::run_whatsapp_sender_worker(state.db.clone(), api.clone(), master_key.clone())`.
  6. **Shared worker deps** (construct once, above the two spawns): `let api: std::sync::Arc<dyn whatsapp::api::WhatsAppApi> = std::sync::Arc::new(whatsapp::api::GraphWhatsAppApi::new());` and `let master_key = std::sync::Arc::new(<MasterKey>);` where `<MasterKey>` is built from config exactly like `integrations::routes::load_master_key` does (`backend/crates/modules/integrations/src/routes.rs:91–99`: `integrations::crypto::MasterKey::from_base64(<the same config field load_master_key reads>)`). If that config value is missing, log a warning and skip spawning the two workers (do not panic) — mirror the S3 fallback style already in main.rs. `storage` is `Arc<dyn ObjectStorage>` already in scope. Verify with `cargo check -p server`.

- [X] T040 [HIGH] Add the WhatsApp reply guards + outbound emit to the human-agent reply path (orig T026 — currently absent; `whatsapp_window_expired`/`whatsapp_channel_disconnected`/`whatsapp_body_too_long` appear nowhere in backend, and `emit_whatsapp_outbound_in_tx` is never called from the reply route). Edit `backend/crates/modules/conversations/src/routes.rs` in the add-message handler (the block that calls `queries::add_message_in_tx`, ~line 720; `conv_channel` is already fetched just above at ~line 704). When `payload.kind == model::MessageKind::Reply` AND `conv_channel == "whatsapp"`, BEFORE inserting, return a `422` ApiError (match the existing ApiError/validation construction style in this file, e.g. `ApiError::unprocessable_entity`/validation code helper used elsewhere):
  - code `whatsapp_channel_disconnected` if `integrations::queries::active_connection_for_slug(&mut *tx or pool, ctx.tenant_id, "whatsapp").await?` is `None`;
  - code `whatsapp_window_expired`, message "The WhatsApp messaging window has expired for this conversation." if `conversations::queries::last_customer_message_at(...) ` for this conversation is `None` or older than 24h (`chrono::Utc::now() - ts > chrono::Duration::hours(24)`);
  - code `whatsapp_body_too_long` if `body.chars().count() > 4096`.
  Then AFTER a successful `add_message_in_tx` for a whatsapp reply, call `conversations::outbox::emit_whatsapp_outbound_in_tx(&mut tx, ctx.tenant_id, conversation_id, message.id).await` inside the same transaction (mirror the existing `emit_customer_message_in_tx` block right below the insert at ~line 744). This makes the existing `backend/crates/server/tests/whatsapp_reply_guards.rs` pass. Run `cargo test -p conversations` and the server reply-guard test.

- [X] T041 [HIGH] Forward the `conversation.message_status` outbox event to the tenant SSE stream (orig T030 — currently `escalations/src/events.rs` has no `message_status` handling, so delivery-status updates never reach the dashboard live). In `backend/crates/modules/escalations/src/events.rs`: (1) add `'conversation.message_status'` to the worker's claimed `event_type` list in the SELECT that claims outbox events (near line 279); (2) add a match arm that serializes it as SSE event name `conversation.message_status` with the payload `{conversationId, messageId, status, failureReason?}` — copy the shape of an adjacent arm exactly. Run `cargo test -p escalations`.

- [X] T042 [HIGH] Add the timeline fields to the frontend message model (orig T020 — the backend already serializes `attachments` and `delivery`, but the TS interfaces omit them). In `frontend/apps/dashboard/src/app/core/api/tenant-api.models.ts`, add to `interface Message` (line ~438) and its wire counterpart `interface MessageWire` (line ~524) these optional fields, typed to the contract in `specs/029-whatsapp-channel/contracts/whatsapp-api.md` §2:
  `attachments?: { id: string; kind: 'image'|'audio'|'video'|'document'; status: 'pending'|'stored'|'failed'; mimeType: string|null; sizeBytes: number|null; fileName: string|null; url: string|null }[]` and
  `delivery?: { status: 'pending'|'sent'|'delivered'|'read'|'failed'; failureReason: string|null }`.
  If the wire→model mapping is done in `conversations-api.service.ts`, pass both fields through there too. Both come from the backend already camelCased, so map 1:1.

- [X] T043 [HIGH] Render inbound attachments in the conversation thread (orig T022 — the standalone `message-attachment.component.ts` exists but is imported ONLY by its own spec; nothing renders it). In `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-thread.component.ts`: import `MessageAttachmentComponent` from `./message-attachment.component`, add it to the component's `imports:` array, and in the message-row template render one `<app-message-attachment>` (use its actual selector — check the `@Component` decorator in `message-attachment.component.ts`) per entry in `message.attachments` when that array is present and non-empty. Follow the existing pattern for how `CitationListComponent` is imported and conditionally rendered in the same file. Run `npx nx test dashboard` for this component.

- [X] T044 [HIGH] Show outbound delivery status ticks and keep them live over SSE (orig T031 — no tick rendering exists and no `message_status` SSE subscription exists). Two parts:
  1. In `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-thread.component.ts`, for outbound messages (`kind` `reply` or `ai`) that have a `delivery` field, render a small status indicator: `pending`→clock, `sent`→✓, `delivered`→✓✓, `read`→✓✓ in an accent color, `failed`→⚠ with `delivery.failureReason` shown inline or as a title/tooltip. Keep it a tiny presentational piece inside the existing message row; reuse existing typography/color tokens.
  2. In `frontend/apps/dashboard/src/app/features/tenant/conversations/conversation-detail.store.ts`, subscribe to the tenant realtime SSE event named `conversation.message_status` using the same realtime client already used for other events (find it by searching the dashboard app for `escalation.assigned`). On each event `{conversationId, messageId, status, failureReason}`, patch the matching message's `delivery` in store state (`{status, failureReason}`). Run `npx nx test dashboard`.

- [X] T045 [MEDIUM] Surface the three WhatsApp reply errors in the composer (orig T032). In `frontend/apps/dashboard/src/app/features/tenant/conversations/composer.component.ts` (and whatever store method handles send failures), map the three `422` ApiError codes from `specs/029-whatsapp-channel/contracts/whatsapp-api.md` §2 to inline human-readable messages, following however the composer surfaces validation errors today: `whatsapp_window_expired`→"The WhatsApp messaging window has expired for this conversation."; `whatsapp_channel_disconnected`→"The WhatsApp channel is disconnected — reconnect it to reply."; `whatsapp_body_too_long`→"Message is too long for WhatsApp (4096 character max)." Run `npx nx test dashboard`.

**Convergence validation**: after these, re-run `cargo test -p whatsapp -p conversations -p customers -p integrations -p ai -p escalations` and the `server` crate's `whatsapp_reply_guards` test with PostgreSQL up, `npx nx test dashboard`, then start the server and walk `quickstart.md` §§1–4 against the live binary (not just tests) to confirm the webhook responds and workers run. Do NOT touch pre-existing repo-wide `cargo fmt` differences; format only files this phase changed.
